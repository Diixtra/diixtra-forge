apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-memory
  namespace: mcp-servers
  labels:
    app.kubernetes.io/name: mcp-memory
    app.kubernetes.io/part-of: mcp-servers
    app.kubernetes.io/managed-by: flux
spec:
  replicas: 1
  strategy:
    type: Recreate # PVC is ReadWriteOnce — old pod must terminate before new one starts
  selector:
    matchLabels:
      app.kubernetes.io/name: mcp-memory
  template:
    metadata:
      labels:
        app.kubernetes.io/name: mcp-memory
        app.kubernetes.io/part-of: mcp-servers
        app.kubernetes.io/managed-by: flux
    spec:
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      containers:
        - name: mcp-memory
          image: node:24-alpine
          command:
            - sh
            - -ec
            - exec npx -y supergateway --stdio "npx -y @modelcontextprotocol/server-memory" --port 8080 --healthEndpoint /healthz
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            - name: HOME
              value: "/tmp"
            # MEMORY_FILE_PATH may be ignored by some versions of
            # @modelcontextprotocol/server-memory (upstream issue #1018).
            # Verify writes land in /data/ after deployment.
            - name: MEMORY_FILE_PATH
              value: "/data/memory.jsonl"
          volumeMounts:
            - name: memory-data
              mountPath: /data
          startupProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 30
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            periodSeconds: 10
            failureThreshold: 3
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 256Mi
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
      volumes:
        - name: memory-data
          persistentVolumeClaim:
            claimName: mcp-memory-data
