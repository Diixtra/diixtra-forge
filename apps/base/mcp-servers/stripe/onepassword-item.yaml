apiVersion: onepassword.com/v1
kind: OnePasswordItem
metadata:
  name: stripe-api-key
  namespace: mcp-servers
  labels:
    app.kubernetes.io/part-of: mcp-servers
    app.kubernetes.io/managed-by: flux
spec:
  itemPath: "vaults/${OP_VAULT}/items/${OP_ITEM_STRIPE_API_KEY}"
