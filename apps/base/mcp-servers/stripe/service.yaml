apiVersion: v1
kind: Service
metadata:
  name: mcp-stripe
  namespace: mcp-servers
  labels:
    app.kubernetes.io/name: mcp-stripe
    app.kubernetes.io/part-of: mcp-servers
    app.kubernetes.io/managed-by: flux
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: mcp-stripe
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30104
      protocol: TCP
