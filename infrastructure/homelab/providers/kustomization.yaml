apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Crossplane provider packages — these Provider CRs use the
  # pkg.crossplane.io/v1 CRD (installed by Crossplane in infrastructure-crds)
  # and register provider-specific CRDs (ProviderConfig, managed resources)
  # needed by the infrastructure stage.
  - ../../base/crossplane-providers
