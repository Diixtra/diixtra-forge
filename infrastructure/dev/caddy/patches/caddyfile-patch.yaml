apiVersion: v1
kind: ConfigMap
metadata:
  name: caddy-config
  namespace: caddy-system
data:
  Caddyfile: |
    {
      email james@kazie.co.uk
    }

    # Dev cluster — local development endpoints
    :80 {
      respond "Dev Caddy is running" 200
    }
