# Packer variables with secrets (API tokens, passwords)
variables.auto.pkrvars.hcl
*.auto.pkrvars.hcl
!*.auto.pkrvars.hcl.example

# Packer build artifacts
output/
packer_cache/

# Plugin binaries (downloaded by `packer init`)
.packer.d/

# Crash logs
crash.log

# macOS
.DS_Store
